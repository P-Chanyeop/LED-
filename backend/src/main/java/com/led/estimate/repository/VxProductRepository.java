package com.led.estimate.repository;

import com.led.estimate.entity.VxProduct;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VxProductRepository extends JpaRepository<VxProduct, Long> {
}
