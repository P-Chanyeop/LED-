server:
  port: 8080

spring:
  application:
    name: led-estimate-backend

# Frontend Auto-Start 설정
frontend:
  auto-start: true  # main() 실행 시 자동으로 npm run dev 실행
  project-root: ".."  # backend 기준 상위 디렉토리

google:
  sheets:
    id: ${GOOGLE_SHEETS_ID:your-spreadsheet-id}

cors:
  allowed-origins: "*"
